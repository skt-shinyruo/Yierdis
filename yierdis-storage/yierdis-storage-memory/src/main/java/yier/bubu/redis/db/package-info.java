/**
 * Concrete in-memory storage implementation owned by {@code yierdis-storage-memory}.
 *
 * <p>This is a legacy package retained for migration compatibility while the
 * Maven module boundary moves out of {@code yierdis-core-db}. Command,
 * protocol, executor, and server modules must continue to depend on
 * {@code yierdis-storage-api} instead of importing these implementation types.
 */
package yier.bubu.redis.db;
