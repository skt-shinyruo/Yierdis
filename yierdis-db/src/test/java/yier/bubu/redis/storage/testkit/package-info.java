/**
 * Storage API test fixtures only. This module must stay independent from
 * concrete storage implementations so each adapter can reuse the same fixtures.
 */
package yier.bubu.redis.storage.testkit;
