# CONTEXT — Yierdis 领域词汇表

本文件是 Yierdis 的**领域词汇表**：只定义术语，不含实现细节，也不记录设计决策。
设计决策记录在 `docs/adr/`；术语到源码的对照表见 `docs/project-docs/glossary.md`。

## 日志（Logging）

### 日志级别

调用点可使用的严重性名称，取值只有 `DEBUG`、`INFO`、`WARNING`、`ERROR`。
拼写沿用 JDK `System.Logger.Level`，因此是 `WARNING` 而不是 `WARN`。

**避用**：`WARN`——它是 `WARNING` 的同义词，只在具体日志框架的配置词汇里出现。

### `FATAL`

不是一个日志级别，而是一个**操作**：记录一条错误日志，并触发现有有序关闭流程。
它表达"进程无法继续"，因此同时具有日志语义和控制流语义。

**避用**：把 `FATAL` 当作"比 `ERROR` 更严重"的纯严重性标签、与 `ERROR` 并列使用。

### 负载（payload）

请求中承载用户数据的部分：命令参数、键名、值。
负载属于敏感数据，**不进入日志**。日志只记录结构信息：命令名、参数个数、字节数、连接标识、异常类型、耗时。

**避用**：用"脱敏"或"掩码"描述对负载的处理——本项目的策略是**不记录**，而不是记录后过滤。

### 运维诊断（operational diagnostics）

为操作者在故障期间回答"进程为什么行为异常"而记录的信息：启动与关闭、连接错误、协议错误、后台维护失败、未捕获异常、DB 不变量失败。

### 请求级审计（request-level audit）

为回答"服务被如何使用"而按请求记录的信息。与运维诊断的两点区别：它按请求产生，量级随吞吐增长；它描述的是请求流的形状，而不是进程的健康状况。
